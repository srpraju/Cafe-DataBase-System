import { <% if(changeDetection !== 'Default') { %>ChangeDetectionStrategy, <% }%>Component<% if(!!viewEncapsulation) { %>, ViewEncapsulation<% }%> } from '@angular/core';<% if(standalone) {%>
import { CommonModule } from '@angular/common';<% } %>
import { sampleData } from './<%= dasherize(name) %>.sample-data';
<% if (chartType == "circular") { %>
import { SeriesLabelsContentArgs} from '@progress/kendo-angular-charts';<% } %><% if (legend) {%>
import { LegendTitle <% if(standalone) {%>, ChartModule<%}%>} from '@progress/kendo-angular-charts';<% } %>
import "hammerjs";

@Component({<% if(!skipSelector) {%>
  selector: '<%= selector %>',<%}%><% if(standalone) {%>
  standalone: true,
  imports: [CommonModule<% if(standalone) {%>, ChartModule<%}%>],<%}%><% if(inlineTemplate) { %>
  template: `<%= include("chart.html.include") %>`<% } else { %>
  templateUrl: './<%= dasherize(name) %><%= type ? '.' + dasherize(type): '' %>.html'<% } if(inlineStyle) { %>,
  styles: [<% if(displayBlock){ %>
    `
      :host {
        display: block;
      }
    `<% } %>
  ]<% } else if (style !== 'none') { %>,
  styleUrls: ['./<%= dasherize(name) %><%= type ? '.' + dasherize(type): '' %>.<%= style %>']<% } %><% if(!!viewEncapsulation) { %>,
  encapsulation: ViewEncapsulation.<%= viewEncapsulation %><% } if (changeDetection !== 'Default') { %>,
  changeDetection: ChangeDetectionStrategy.<%= changeDetection %><% } %>
})
export class <%= classify(name) %><%= classify(type) %> {
    public seriesData = sampleData;
<% if (legend) { %>
    public title: LegendTitle = { text: "<%= capitalize(name) %>" };
<% } %>
<% if (chartType == "stock" && navigator) { %>
    public navigatorFrom: Date = <%=navigatorFrom%>;
    public navigatorTo: Date = <%=navigatorTo%>;
<% } %>
}